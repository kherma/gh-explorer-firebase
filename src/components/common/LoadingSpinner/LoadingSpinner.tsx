import React from 'react';

const LoadingSpinner = () => (
  <span className="inline-block w-full h-full border-4 border-t-4 border-green-400 rounded-full animate-spin loading-spinner-border-tlr"></span>
);

export default LoadingSpinner;
